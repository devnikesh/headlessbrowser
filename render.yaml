services:
  - type: web
    name: figma-headless-proxy
    runtime: node
    plan: free
    buildCommand: npm install --production
    startCommand: node headless-server.js
    envVars:
      - key: NODE_ENV
        value: production
